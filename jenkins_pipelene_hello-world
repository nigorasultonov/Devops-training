pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker pull hello-world'
            }
        }
        stage('Run') {
            steps {
                sh 'docker run hello-world'
            }
        }
    }
}
